services:
  cache:
    image: redis:7.4-alpine
    container_name: red
    restart: always
    ports:
      - "6379:6379"
    command: redis-server --requirepass red
    volumes:
      - cache:/data
volumes:
  cache:
    driver: local