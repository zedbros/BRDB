services:
  db:
    image: postgres:latest
    container_name: post
    restart: always
    ports:
      - "5432:5432"
    volumes:
     - type: tmpfs
       target: /dev/shm
    environment:
      POSTGRES_USER: post
      POSTGRES_PASSWORD: post